spring:
  datasource:
    url:jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto:update 
    show-sql: false
  mail:
    host: localhost
    port: 1025

logging:
  level:
    org.springframework: WARN

sql:
  init: 
    mode: never